import { Router } from 'express'; import { body,query,param } from 'express-validator'; import { makeCrud } from '../controllers/crudController.js'; import { authenticate,authorize } from '../middleware/auth.js'; import { validate } from '../middleware/validate.js';
import Doctor from '../models/Doctor.js'; import Patient from '../models/Patient.js'; import Department from '../models/Department.js'; import Bed from '../models/Bed.js'; import Appointment from '../models/Appointment.js';
const router=Router();
const common=(Model,fields)=>{const crud=makeCrud(Model,{searchFields:fields});router.get(`/${Model.modelName.toLowerCase()}s`,authenticate,[query('page').optional().isInt({min:1}),query('limit').optional().isInt({min:1,max:100}),query('search').optional().trim()],validate,crud.list);router.get(`/${Model.modelName.toLowerCase()}s/:id`,authenticate,param('id').isMongoId().withMessage('Invalid id'),validate,crud.get);router.post(`/${Model.modelName.toLowerCase()}s`,authenticate,authorize('super_admin','hospital_admin','department_manager'),crud.create);router.put(`/${Model.modelName.toLowerCase()}s/:id`,authenticate,authorize('super_admin','hospital_admin','department_manager'),param('id').isMongoId(),validate,crud.update);router.delete(`/${Model.modelName.toLowerCase()}s/:id`,authenticate,authorize('super_admin','hospital_admin'),param('id').isMongoId(),validate,crud.remove)};
common(Doctor,['name','email','specialization','department']);common(Patient,['name','email','condition']);common(Department,['name','code','specialty']);common(Bed,['bedNumber','department','type','status']);common(Appointment,['patientName','doctorName','status']);
export default router;
