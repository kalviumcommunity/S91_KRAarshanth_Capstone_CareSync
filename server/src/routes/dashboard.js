import { Router } from 'express'; import { authenticate } from '../middleware/auth.js'; import Doctor from '../models/Doctor.js'; import Patient from '../models/Patient.js'; import Bed from '../models/Bed.js'; import Appointment from '../models/Appointment.js';
const router=Router();router.get('/summary',authenticate,async(req,res,next)=>{try{const [patients,doctors,totalBeds,availableBeds,occupiedBeds]=await Promise.all([Patient.countDocuments(),Doctor.countDocuments(),Bed.countDocuments(),Bed.countDocuments({status:'Available'}),Bed.countDocuments({status:'Occupied'})]);const start=new Date();start.setHours(0,0,0,0);const end=new Date(start);end.setDate(end.getDate()+1);const todayAppointments=await Appointment.countDocuments({date:{$gte:start,$lt:end}});res.json({success:true,patients,doctors,totalBeds,availableBeds,occupiedBeds,todayAppointments,bedUtilization:totalBeds?Math.round((occupiedBeds/totalBeds)*100):0})}catch(e){next(e)}});export default router;
